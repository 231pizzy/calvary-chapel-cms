'use client'

import CMSLayout from "@/Components/CMSLayout";
import {
    Box,
} from "@mui/material";

export default function About() {

    return (
        <CMSLayout menuId='about' pageTitle={"About CCT"}>
            About page
        </CMSLayout>
    );
} 