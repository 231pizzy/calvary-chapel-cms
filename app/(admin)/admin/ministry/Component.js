'use client'

import CMSLayout from "@/Components/CMSLayout";
import {
    Box,
} from "@mui/material";

export default function Ministry() {

    return (
        <CMSLayout menuId='ministry' pageTitle={"Ministry"}>
            ministry
        </CMSLayout>
    );
} 